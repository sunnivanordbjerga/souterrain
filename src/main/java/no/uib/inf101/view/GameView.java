package no.uib.inf101.view;

import no.uib.inf101.model.game.node.Node;

import javax.swing.*;

/**
 * Renders the current game state by delegating to
 * the {@link StoryPanel}, {@link ChoicePanel} and {@link ScenePanel}.
 */
public class GameView {
    //TODO GameView

    /**
     * Creates a new {@link GameView}.
     *
     * @param game the game to render
     */
    public GameView(ViewableGame game) {
    }
}
